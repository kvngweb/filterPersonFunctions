# TOPIC: filterPersons function 

It returns User[] when personType is 'user'.

It returns Admin[] when personType is 'admin'.

# The function accept partial User or Admin types based on the personType argument. 

For example:

If personType is 'user', the criteria argument accept a partial User object.

If personType is 'admin', the criteria argument accept a partial Admin object.
